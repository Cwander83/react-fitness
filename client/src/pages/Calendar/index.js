import React from "react";
import "../../styles/style.css";

const Calendar = ({ match }) => {
   console.log(match.url);
   return <h1>Hello world this is Calender</h1>;
};

export default Calendar;
