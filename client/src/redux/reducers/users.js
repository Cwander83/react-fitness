const initialState = {
    data: []
}

const users = (state = initialState, action)=> {
    switch(action.type){
        case '':
        return {
            ...state
        }
    }
}

export default users;